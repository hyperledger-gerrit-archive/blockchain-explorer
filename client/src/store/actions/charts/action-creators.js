/*import { createAction } from 'redux-actions'
import * as actionTypes from './action-types'

export const getPeerList = () => dispatch => {
    dispatch(createAction(actionTypes.PEER_LIST)('PEER_LIST'));
}

export function setPeerList(peers) {
    return {
      type: PEER_LIST,
      peers
    }
  }
*/



  
